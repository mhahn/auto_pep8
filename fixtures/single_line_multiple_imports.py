from os import getcwd, chown, mkdir, pipe, popen
import sys
from sys import flags, maxint, maxsize

from numbers import Number, Real, Number, Integral, Complex, Complex

from decimal import DecimalTuple, \
        OverFlow

from string import capwords, find, \
        hexdigits

from threading import deque, \
        active_count, \
        stack_size, \
        warnings, \
        settrace

getcwd
mkdir
popen
maxint
OverFlow
capwords
find
stack_size
Real
Complex


def within_a_function():
    from random import choice, sample, shuffle
    choice
    sample
